<?xml version="1.0" encoding="utf-8"?>
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/flow_tag"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    android:background="@drawable/flow_selector"
    android:gravity="center"
    android:textSize="15dp"
    android:paddingTop="@dimen/safe_padding"
    android:paddingBottom="@dimen/safe_padding"
    android:paddingStart="@dimen/safe_padding"
    android:paddingEnd="@dimen/safe_padding"
    android:textColor="@color/colorPrimary"
    tools:text="@string/app_name" />